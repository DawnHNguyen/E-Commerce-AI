<!-- res/color/recurly_box_stroke_color.xml -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- focused: use your text‑field background color as stroke -->
    <item android:state_focused="true"
        android:color="@color/colorSystem_text_field" />
    <!-- default: transparent -->
    <item android:color="@android:color/transparent" />
</selector>
